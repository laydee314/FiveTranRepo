name: 'retail_analytics'
version: '1.0.0'
config-version: 2

profile: 'retail_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  retail_analytics:
    marts:
      +materialized: table
      +schema: analytics
```

**`.gitignore`**
```
# dbt
target/
dbt_packages/
logs/

# Never commit profiles.yml — contains credentials
profiles.yml
